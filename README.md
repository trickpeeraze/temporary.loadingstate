# enegist.loadingstate

- - -

*lastest version: 1.1.1*

A utility for showing loading state of a process on the web

for an example;
``` javascript
var state = $('submit-button').loadingstate();

progress_something = function(){
	state.start();
}

```